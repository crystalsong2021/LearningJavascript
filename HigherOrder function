each
Description: Underscore _.each(list, iteratee, [context]) Alias: forEach
Iterate over a list of elements, yeilding each in turn to an iteratee function. The iteratee bound to the context object, if one is passed. 
Each invocation of iteratee is called with three arguments:(element, index, list). If list is a javascript object, iteratee's arguments will be
(value, key, list). Returns the list for chaining.
